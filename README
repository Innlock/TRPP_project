Тут будет 4 ветки:
client (для фронта), server (сервер), database (для бд), master (основная ветка проекта).
Просьба первое время работать в своей ветке.
Чтобы было проще ориентироваться, на каждой ветке (client, server, database) оставлю по картинке.